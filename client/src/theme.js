import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#25274d",
    },
    secondary: {
      main: "#309acd",
    },
    background: "#25274d",
    lightBackground: "#474866",
    lightTitle: "#309acd",
  },
});
export default theme;
